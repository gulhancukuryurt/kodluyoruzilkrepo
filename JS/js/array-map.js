const users = ["Gülhan", "Gülşah","Selim", "Sena"]

const newUsers = users.map(user => user.toUpperCase())
console.log(newUsers)

const userObj = uses.map(item =>
    {

        return {
            userName: item, shortName: `${item[0]}`,newName: `${item[0].toUpperCase()}${item.slice(1).toLoweCase()}}`
        }


    }
    
    
    
    )